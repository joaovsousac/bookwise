import { styled } from "@stitches/react";

export const Container = styled("div", {
    display: "flex",
    gap: "$5",
  
    svg: {
      width: 32,
      height: 32,
      color: "$green-100"
    }
  })